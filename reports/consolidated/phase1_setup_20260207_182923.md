# Phase 1 - Preparation outillage + dependances + dossiers
Timestamp: 20260207_182923_EuropeParis

## Faits (preuves/logs)
- Dossiers crees (ou assures): `reports/raw`, `reports/consolidated`, `tools`, `perf`.
- `requirements.txt` mis a jour pour ajouter `pytest-cov`.
- Commande uvicorn detectee: `uvicorn src.main:app --reload` (see `README.md` et `src/main.py`).

## Diff requirements/pyproject
```diff
@@ -14,6 +14,7 @@ cryptography==46.0.3
 
 # --- Dev / tests / qualite ---
 pytest==9.0.2
+pytest-cov==5.0.0
 httpx==0.28.1
 ruff==0.14.13
 flake8==7.1.1
```

## Conventions d'installation (venv + deps)
- `python -m venv .venv`
- Activation PowerShell: `\.venv\Scripts\Activate.ps1`
- `python -m pip install -r requirements.txt`

## Interpretation
- L'outillage QA minimal est present (pytest/flake8/pylint/radon) et `pytest-cov` complete la couverture.

## Recommandations
- Garder les versions des outils QA figees pour la reproducibilite.
- Ajouter une verification rapide `python -m pytest -q` en local avant CI.

