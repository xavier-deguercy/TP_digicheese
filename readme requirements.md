Dépendances (requirements)
Base qu j'ai comparé au tp 7 :

fastapi
sqlmodel
uvicorn[standard]
python-dotenv
pymysql
pytest
httpx
ruff

Mise à jour pip + install :

python -m pip install --upgrade pip
pip install -r requirements.txt
